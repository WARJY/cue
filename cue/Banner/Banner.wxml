<view class='banner-box'>
  <swiper class="my-class" indicator-dots="{{true}}" autoplay="{{autoplay}}" interval="{{1000}}" duration="{{500}}"  style='height:{{height+"rpx"}}'>
    <block wx:for="{{Items}}" wx:key="id">
      <swiper-item>
        <image class='image' src="{{item.src}}" />
      </swiper-item>
    </block>
  </swiper>
</view>